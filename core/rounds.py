def get_rounds_for_fund(fund_id):
    return f"Worked, here's the rounds for this fund with id: {fund_id}"

def get_round(fund_id, round_id):
    return f"here's your round with id: {round_id} from fund id: {fund_id}"
